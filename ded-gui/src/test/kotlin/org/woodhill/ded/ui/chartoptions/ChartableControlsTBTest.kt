package org.woodhill.ded.ui.chartoptions

import org.junit.Before
import org.junit.Test

class ChartableControlsTBTest {

    @Before
    fun setUp() {

    }

    @Test
    fun hasLogCharts() {
        println("Running hasLogCharts test")
    }

    @Test
    fun isVisible() {
        println("Running isVisible test")
        // TODO Now ChartableControlsTB is initailized with a model ID, a JDDEObject and a KChartableProperties obj.
//        val chartableControls = ChartableControlsTB(1, )
//        assertFalse(chartableControls.isVisible("NotAVar"))
//
//        val newVar:String = "newVar"
//        // TODO Doesn't look like oparam type is used here.
//        chartableControls.updateProperty(newVar, ParameterChartOptions(newVar, true, false, ParameterChartOptions.DisplayStyle.CURVE, ParameterChartOptions.ParamType.VARIABLE))
//        assertTrue(chartableControls.isVisible(newVar))
//        assertFalse(chartableControls.isLog(newVar))
//        assertEquals(chartableControls.getDisplayStyle(newVar), ParameterChartOptions.DisplayStyle.CURVE)
//        //assertEquals(chartableControls.)


    }

    @Test
    fun testIsVisible() {
    }

    @Test
    fun contains() {
    }

    @Test
    fun getDisplayStyle() {
    }

    @Test
    fun isLog() {



    }

    @Test
    fun isData() {
    }

    @Test
    fun getVisibleDataColumns() {
    }
}